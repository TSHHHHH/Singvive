/** Hover delay for fine pointers. Shared by TipLayer and item peek. */
export const HOVER_DELAY_MS = 120;
/** Press-and-hold before a coarse pointer pins a tip or item card. */
export const LONG_PRESS_MS = 450;
/** Finger travel (px) that cancels a pending hold — pan / drag wins. */
export const HOLD_MOVE_PX = 8;
